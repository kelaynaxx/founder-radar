"""Small text helpers shared across the codebase.

Keep this module dependency-free on purpose. Anything that needs an external
library (language detection, embeddings, ...) belongs in its own module.
"""